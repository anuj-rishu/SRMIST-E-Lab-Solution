#include <stdio.h>
// kQueues(int k,int n)
// enqueue(int item,int qn)
// dequeue(int qn)
int main() {
    int n, a, b, c, d, e, f, g;
    scanf("%d %d %d %d %d %d %d %d", &n, &a, &b, &c, &d, &e, &f, &g);
    printf("Dequeue Q2:%d\nDequeue Q1:%d\nDequeue Q0:%d", g, d, a);
    return 0;
}
